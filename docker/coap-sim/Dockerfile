# REMOVED: CoAP is UDP-only (port 5683/udp). Cloudflare Workers connect() is TCP-only.
# The CoAP worker handler (src/worker/coap.ts) uses RFC 8323 CoAP-over-TCP, but this
# simulator only supports UDP. This directory can be safely deleted.
