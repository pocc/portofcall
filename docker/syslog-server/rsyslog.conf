# rsyslog configuration for test syslog receiver

module(load="imudp")
input(type="imudp" port="514")

module(load="imtcp")
input(type="imtcp" port="514")

# Log everything to stdout for container logging
*.* /dev/stdout

# Also log to file
*.* /var/log/messages
