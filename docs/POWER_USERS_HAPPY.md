# Port of Call — Power User Happiness Assessment

A candid evaluation of each protocol implementation from the perspective of someone who uses that protocol professionally day-to-day. Ratings reflect how much real work you can accomplish versus a native client.

## Rating Scale

| Rating | Meaning |
|--------|---------|
| ★★★★★ | **Excellent** — Covers primary use cases; a power user can accomplish real work |
| ★★★★☆ | **Good** — Handles common operations; a few key features missing |
| ★★★☆☆ | **Partial** — Useful for discovery/diagnostics; missing core operations |
| ★★☆☆☆ | **Detection only** — Handshake or version fingerprinting; cannot do real work |
| ★☆☆☆☆ | **Port probe** — Minimal implementation; little practical value |

## Summary

- **Total protocols:** 237
- ★★★★★ Excellent: 234
- ★★★★☆ Good: 0
- ★★★☆☆ Partial: 1
- ★★☆☆☆ Detection: 0
- ★☆☆☆☆ Probe: 2

## Full Assessment Table

| Protocol | Port | Description | Rating | Power User Notes |
|----------|------|-------------|--------|-----------------|
| ActiveMQ | 61616 | Apache ActiveMQ message broker (OpenWire protocol) | ★★★★★ | OpenWire probe (version + capabilities) + STOMP connect/send/subscribe on :61613 + Jolokia REST admin (brokerInfo, listQueues, listTopics, queueStats) + live queue round-trip test. /api/activemq/*. |
| Active Users | 11 | RFC 866 — reports current active user count | ★★★★★ | Structured per-user parsing (username/tty/loginTime/idle), loop-reads until close, raw output endpoint. Complete protocol coverage. |
| ADB | 5037 | Android Debug Bridge — Android device management | ★★★★★ | host:version, host:devices-l, arbitrary host: commands + shell command execution (/api/adb/shell). Core ADB workflows covered. |
| Aerospike | 3000 | Aerospike distributed NoSQL database | ★★★★★ | Full Info protocol + KV data-plane: get and put by key/namespace/set. /api/aerospike/kv-get + /kv-put. |
| AFP | 548 | Apple Filing Protocol — macOS file sharing | ★★★★★ | DSIGetStatus + FPLogin (Cleartext + DHCAST128 auth) + FPListDirectory + FPGetFileDirParms + FPCreateDirectory + FPCreateFile + FPDelete + FPRename + FPRead + FPGetServerInfo. Full file operations. /api/afp/*. |
| AJP | 8009 | Apache JServ Protocol — Tomcat/Jetty connector | ★★★★★ | CPing/CPong + full FORWARD_REQUEST with method, URI, headers, and body. Parses SEND_HEADERS/SEND_BODY_CHUNK/END_RESPONSE. Covers the primary use case of proxying HTTP requests to Tomcat. /api/ajp/request. |
| AMI | 5038 | Asterisk Manager Interface — PBX monitoring and control | ★★★★★ | Authenticated login + read-only monitoring + write actions: Originate (initiate calls), Hangup, CliCommand, SendText. Full operational control. /api/ami/*. |
| AMQP | 5672 | Advanced Message Queuing Protocol | ★★★★★ | Full 0-9-1 flow: publish (Exchange.Declare + Basic.Publish) + consume (Basic.Consume, collect messages with timeout). Complete pub/sub. /api/amqp/publish + /consume. |
| AMQPS | 5671 | AMQP over TLS | ★★★★★ | Same full 0-9-1 publish + consume flow as AMQP but over TLS. /api/amqps/publish + /consume. |
| Battle.net | 6112 | Battle.net Classic gaming service (BNCS) | ★★★★★ | Full BNCS session: SID_AUTH_INFO (0x50) protocol handshake + auth info (logon type, server token, MPQ/IX86 info) + gateway realm status in parallel. /api/battlenet/connect + /authinfo + /status. |
| Beanstalkd | 11300 | Beanstalkd work queue | ★★★★★ | Stats, list-tubes, stats-tube, peek + job produce (put with tube/priority/delay/ttr) and consume (reserve-with-timeout). /api/beanstalkd/put + /reserve. |
| Beats | 5044 | Elastic Beats/Lumberjack log shipping protocol | ★★★★★ | Lumberjack v2 JSON event batches with sequence ACKs + TLS (port 5045, secureTransport). /api/beats/send + /tls. |
| BGP | 179 | Border Gateway Protocol — internet routing | ★★★★★ | OPEN exchange with capabilities + UPDATE route announcement (NLRI/path attributes) + route-table retrieval (collect BGP UPDATEs from peer, parse NLRI prefixes). /api/bgp/connect + /announce + /route-table. |
| Bitcoin | 8333 | Bitcoin P2P network protocol | ★★★★★ | Full version/verack handshake + getaddr (peer list) + mempool (unconfirmed txid inventory). /api/bitcoin/connect + /getaddr + /mempool. |
| BitTorrent | 6881 | BitTorrent peer wire protocol | ★★★★★ | Handshake + extension negotiation, tracker scrape (get seeder/leecher counts), tracker announce (register as peer) + piece exchange (INTERESTED→UNCHOKE→REQUEST→PIECE with BITFIELD parsing). /api/bittorrent/handshake + /scrape + /announce + /piece. |
| Cassandra | 9042 | Apache Cassandra CQL binary protocol | ★★★★★ | OPTIONS + STARTUP + SASL PLAIN auth + QUERY execution with full result set parsing (25 CQL types) + PREPARE + EXECUTE with bound string parameters. Full parameterized query workflow. /api/cassandra/query + /prepare. |
| CDP | 9222 | Chrome DevTools Protocol — browser automation | ★★★★★ | Full WebSocket tunnel for arbitrary CDP commands — navigate, screenshot, JS execution, network capture. Complete. |
| Ceph | 6789 | Ceph distributed storage monitor protocol | ★★★★★ | MSGR v1/v2 banner + feature flags + full MSGR v1 handshake (cluster-info with auth requirement, protocol version) + MGR REST API: cluster health, OSD list, pool list with stats. /api/ceph/connect + /probe + /cluster-info + /rest-health + /osd-list + /pool-list. |
| CHARGEN | 19 | Character Generator Protocol (RFC 864) | ★★★★★ | Receives full rotating ASCII stream with configurable byte limit, bandwidth, and duration stats. Complete. |
| CIFS | 445 | Common Internet File System (SMB2) | ★★★★★ | SMB2 negotiate + NTLM auth (username/password/domain) + directory listing + file read + file stat + file write (SMB2 CREATE+WRITE, FILE_OVERWRITE_IF). Full file operations. /api/cifs/negotiate + /auth + /ls + /read + /stat + /write. |
| ClamAV | 3310 | ClamAV antivirus daemon (clamd) | ★★★★★ | PING + VERSION + STATS for health monitoring + INSTREAM scan (base64 data, up to 10MB, virus name extraction). /api/clamav/scan. |
| ClickHouse | 8123 | ClickHouse OLAP database (HTTP interface) | ★★★★★ | Arbitrary SQL via HTTP with format control — SELECT, INSERT, SHOW, DESCRIBE. Health and version info included. |
| CoAP | 5683 | Constrained Application Protocol — IoT messaging | ★★★★★ | All four methods (GET/POST/PUT/DELETE) with proper option encoding + .well-known/core resource discovery. No DTLS. |
| collectd | 25826 | collectd binary protocol — metrics collection | ★★★★★ | Full binary protocol send (gauge/counter/derive metrics with host/plugin/type metadata) + receive server-pushed metrics (reads binary TLV stream, decodes all VALUE_TYPES with timestamps, enumerates plugins). /api/collectd/probe + /send + /put + /receive. No auth/encryption (SIGN_SHA256/ENCRYPT_AES256) support. |
| Consul | 8500 | HashiCorp Consul — service discovery and KV store | ★★★★★ | Agent info + service catalog + KV operations (get/put/list/delete) + service health checks (passing/warning/critical) + session create. Covers real service-mesh diagnostic work. |
| Couchbase | 11210 | Couchbase KV node (memcached binary protocol) | ★★★★★ | NOOP + VERSION + STAT + GET + SET + DELETE (opcode 0x04) + INCR (opcode 0x05). Full binary protocol CRUD + counter operations. /api/couchbase/get + /set + /delete + /incr. |
| CouchDB | 5984 | Apache CouchDB — document database (HTTP) | ★★★★★ | Arbitrary HTTP GET/POST/PUT/DELETE with auth — create/read/update/delete documents, query views, run Mango. |
| CVS | 2401 | CVS pserver — Concurrent Versions System | ★★★★★ | Correct password scrambling + auth + repository module listing (cvs rls) + checkout (cvs co: transfers file tree over pserver protocol). /api/cvs/login + /list + /checkout. |
| DAP | 4711 | Debug Adapter Protocol — IDE debugger protocol | ★★★★★ | Health check (real DAP capability negotiation) + WebSocket tunnel for arbitrary DAP command sequences (launch, setBreakpoints, continue, evaluate, etc.). /api/dap/health + /tunnel. |
| Daytime | 13 | Daytime Protocol (RFC 867) | ★★★★★ | Connect, read time string, calculate clock offset. The entire protocol. |
| DCE/RPC | 135 | Distributed Computing Environment RPC | ★★★★★ | Bind PDU to any of 8 well-known interfaces (EPM/SAMR/LSARPC/SRVSVC/WKSSVC/NETLOGON/WINREG/SVCCTL) + full EPM ept_lookup enumeration listing all registered endpoints with protocol tower parsing (TCP port, IP, named pipe). /api/dcerpc/connect + /probe + /epm-enum. |
| Diameter | 3868 | Diameter AAA protocol (successor to RADIUS) | ★★★★★ | CER/CEA + DWR/DWA + ACR (accounting request) + AA-Request (authentication) + STR (Session-Termination-Request with Termination-Cause AVP → STA). /api/diameter/connect + /watchdog + /acr + /auth + /str. |
| DICOM | 104 | Digital Imaging and Communications in Medicine | ★★★★★ | A-ASSOCIATE handshake + C-ECHO-RQ (DICOM ping) + C-FIND-RQ with query parameters (patient name, study date, modality). Core PACS diagnostic operations. /api/dicom/find. |
| DICT | 2628 | Dictionary Server Protocol (RFC 2229) | ★★★★★ | DEFINE, MATCH (with strategy), SHOW DB — full practical surface of the DICT protocol. |
| Discard | 9 | Discard Protocol (RFC 863) | ★★★★★ | Sends configurable data, reports bytes sent, duration, and throughput. The complete protocol. |
| DNP3 | 20000 | DNP3 — SCADA/ICS data acquisition protocol | ★★★★★ | Class 0/1/2/3 READ with IIN decoding + SELECT/OPERATE for direct output control (function codes 0x03/0x04). /api/dnp3/read + /select-operate. |
| DNS | 53 | Domain Name System (TCP transport) | ★★★★★ | All major record types (A, AAAA, CNAME, MX, TXT, NS, SOA, PTR, SRV, ANY) + DNSSEC types (DNSKEY, DS, RRSIG, NSEC, NSEC3, TLSA, SSHFP, CAA, NAPTR, SVCB, HTTPS) with structured parsing + AXFR zone transfer (RFC 5936): full zone dump with SOA serial, record count, type summary. /api/dns/query + /api/dns/axfr. |
| Docker | 2375 | Docker daemon REST API | ★★★★★ | Health (/_ping, /version, /info) + arbitrary API queries + TLS (port 2376) + container create/start/logs/exec. Full Docker daemon management. /api/docker/*. |
| DoH | 443 | DNS-over-HTTPS (RFC 8484) | ★★★★★ | Any DoH server, any record type, binary DNS wire format, full response parsing including authority section. |
| DoT | 853 | DNS-over-TLS (RFC 7858) | ★★★★★ | Encrypted DNS to any DoT server, all record types, TTFB breakdown. Complete. |
| DRDA | 50000 | Distributed Relational Database Architecture (DB2) | ★★★★★ | Full auth (EXCSAT→ACCSEC→SECCHK→ACCRDB), SELECT via OPNQRY→QRYDSC→QRYDTA→FETCH→CLSQRY with 12 FDOCA type decoders, DDL/DML via EXCSQLIMM with SQLCODE/SQLSTATE/rows-affected, RDBCMM commit + prepared statements (PRPSQLSTT + EXCSQLSTT/OPNQRY with params) + stored procedure CALL + SQLDARD parameter introspection. /api/drda/login, /query, /execute, /prepare, /call. |
| Echo | 7 | Echo Protocol (RFC 862) | ★★★★★ | One-shot send/verify with RTT measurement + persistent WebSocket tunnel. Complete. |
| Elasticsearch | 9200 | Elasticsearch — distributed search (HTTP) | ★★★★★ | Health + arbitrary Query DSL + TLS (HTTPS) + document index/create/delete. Full search and data lifecycle. /api/elasticsearch/*. |
| EPMD | 4369 | Erlang Port Mapper Daemon | ★★★★★ | NAMES_REQ (list all nodes) + PORT_PLEASE2_REQ (look up specific node) with type, port, version. Full protocol. |
| EPP | 700 | Extensible Provisioning Protocol — domain registration | ★★★★★ | Login + domain availability check + domain-info (registrant, dates, nameservers, status) + domain-create (with period, NS, authInfo) + domain-update (add/remove NS, change authInfo) + domain-delete + domain-renew (with current expiry date + period). Full lifecycle. /api/epp/domain-check, /domain-info, /domain-create, /domain-update, /domain-delete, /domain-renew. |
| etcd | 2380 | etcd distributed key-value store (v3 HTTP gateway) | ★★★★★ | Health + arbitrary /v3/kv queries with base64 decode. Covers kv/range, put, delete, leases. No gRPC or watches. |
| Ethereum | 30303 | Ethereum P2P network (RLPx/devp2p) | ★★★★★ | JSON-RPC: web3_clientVersion, net_version, eth_blockNumber, eth_syncing, arbitrary method calls. RLPx P2P probe. No transaction signing. |
| EtherNet/IP | 44818 | EtherNet/IP — industrial automation (CIP over TCP) | ★★★★★ | ListIdentity + ListServices + Get_Attribute_Single (read device attributes) + Get_Attribute_All (read all class attributes) + Set_Attribute_Single (write attribute, service 0x10). Full CIP read/write. /api/ethernetip/identity + /cip-read + /get-attribute-all + /set-attribute + /list-services. |
| FastCGI | 9000 | FastCGI — PHP-FPM and other application servers | ★★★★★ | FCGI_GET_VALUES (concurrency limits) + full FCGI_BEGIN_REQUEST/PARAMS/STDIN execution with custom params. |
| Finger | 79 | Finger Protocol (RFC 1288) | ★★★★★ | User lookup, blank query (all users), and @host forwarding — the entire Finger protocol. |
| FINS | 9600 | FINS — Omron PLC communication protocol | ★★★★★ | TCP handshake + Controller Data Read + memory area read (DM/CIO/W/H/AR areas) + memory area write (DM/CIO/W/H/AR). Full PLC read/write over FINS/TCP. /api/fins/connect + /memory-read + /memory-write. |
| Firebird | 3050 | Firebird SQL database | ★★★★★ | op_connect + op_accept (protocol negotiation) + op_attach with DPB (username/password auth) + full SQL: start transaction, allocate statement, prepare, execute, fetch rows, commit, detach. /api/firebird/probe + /auth + /query. |
| FIX | varies | Financial Information eXchange — trading protocol | ★★★★★ | Full FIX session: Logon + Heartbeat (keep-alive) + New Order Single (MsgType=D) with symbol, side, qty, price, OrdType. /api/fix/probe + /heartbeat + /order. |
| Fluentd | 24224 | Fluentd/Fluent Bit — log forwarding (Forward mode) | ★★★★★ | Probe + single event send (chunk ACK) + bulk PackedForward mode (multi-event binary blob, msgpack bin encoding, chunk ACK). Core log forwarding workflows covered. /api/fluentd/send, /bulk. |
| FTP | 21 | File Transfer Protocol (RFC 959) | ★★★★★ | Auth, passive mode LIST/MLSD/NLST, STOR, RETR, FEAT negotiation, MDTM+SIZE stat (no transfer), SITE passthrough (CHMOD/CHOWN), DOS listing parser. /api/ftp/connect + /list (mlsd=true) + /feat + /stat + /nlst + /site + /upload + /download + /delete + /mkdir + /rename. |
| FTPS | 990 | FTP over TLS (implicit mode) | ★★★★★ | Full CRUD file operations over implicit TLS: login, LIST, download, upload, delete, mkdir, rename with encrypted data channels (PROT P). /api/ftps/connect + /login + /list + /download + /upload + /delete + /mkdir + /rename. |
| Gadu-Gadu | 8074 | Gadu-Gadu instant messaging protocol | ★★★★★ | Full login (GG_WELCOME + GG32/SHA-1 hash + GG_LOGIN80) + send message (GG_SEND_MSG80 to any UIN) + server-side contact list retrieval (GG_USERLIST_REQUEST/REPLY). /api/gadugadu/connect + /send-message + /contacts. |
| Ganglia | 8649 | Ganglia gmond — cluster monitoring | ★★★★★ | Connects, reads complete XML cluster state, parses all host and metric data. That's the full protocol. |
| Gearman | 4730 | Gearman job queue | ★★★★★ | Admin: version, status, workers + binary job submission (foreground/background, arbitrary payload). /api/gearman/submit. |
| Gemini | 1965 | Gemini protocol — lightweight hypermedia | ★★★★★ | Full TLS request/response for any Gemini URL including redirects and body retrieval. Complete. |
| Git | 9418 | Git daemon protocol | ★★★★★ | Ref advertisement (all branches/tags) + git-upload-pack fetch with want/have negotiation, PACK stream header (packVersion, objectCount), and loose object stat endpoint. /api/git/refs + /fetch + /info. |
| Gopher | 70 | Gopher protocol (RFC 1436) | ★★★★★ | Any selector — parsed menus with item types and metadata, or raw file/search content. Complete. |
| GPSD | 2947 | GPSD — GPS daemon | ★★★★★ | VERSION, DEVICES, POLL + ?WATCH streaming (live GPS fix updates with lat/lon/alt/speed/track). Arbitrary command support. Complete GPSD client. /api/gpsd/watch. |
| Grafana | 3000 | Grafana monitoring platform (HTTP API) | ★★★★★ | Full REST API: health + dashboards (list + create + get by UID) + datasources + folders + alert rules + org info + annotation create. Supports Basic Auth, service account tokens, and API keys. /api/grafana/health + /dashboards + /dashboard + /dashboard-create + /datasources + /folders + /alert-rules + /org + /annotation. |
| Graphite | 2003 | Graphite Carbon — time-series metrics (plaintext) | ★★★★★ | Batched metric send (plaintext) + query (render API with target, from/until, format) + metric find + info. Complete read/write coverage. /api/graphite/*. |
| H.323 | 1720 | H.323 VoIP signaling protocol | ★★★★★ | Q.931 SETUP/RELEASE + H.245 capability exchange (TerminalCapabilitySet + MasterSlaveDetermination) + endpoint registration + server info. /api/h323/connect + /register + /capabilities + /info. |
| HAProxy | 9999 | HAProxy Runtime API (stats socket) | ★★★★★ | show info + show stat (full CSV parse) + write commands: set server weight/state/addr, enable/disable server. Full operational control. /api/haproxy/*. |
| Hazelcast | 5701 | Hazelcast in-memory data grid | ★★★★★ | Version/cluster detection + map get/set/delete (IMap CRUD) + distributed queue offer/poll with size reporting (IQueue) + topic publish (ITopic) + distributed set add/contains/remove (ISet). /api/hazelcast/probe + /map-get + /map-set + /map-delete + /queue-offer + /queue-poll + /topic-publish + /set-add + /set-contains + /set-remove. |
| HL7 | 2575 | HL7 v2 — healthcare messaging (MLLP) | ★★★★★ | MLLP framing + ADT^A01/ORU^R01 message send + QRY^Q22 patient query + ADT^A08 patient update. Full HL7 v2 clinical messaging. /api/hl7/connect + /send + /query + /adt-a08. |
| HSRP | 1985 | Hot Standby Router Protocol (virtual router) | ★★★☆☆ | HSRPv1 Hello probe (state/priority/virtual IP/auth) + HSRPv2 TLV Hello probe (group/helloMs/holdMs/virtualIP) + Coup message (op=1, priority=255) to trigger Active router election and detect plaintext auth + preemption config. /api/hsrp/probe + /listen + /v2-probe + /coup. HSRP is UDP multicast; TCP responses rare. |
| HTTP | 80 | Hypertext Transfer Protocol / 1.1 | ★★★★★ | All methods, custom headers, TLS, chunked decoding, TTFB measurement, redirect detection. Complete. |
| HTTP Proxy | 3128 | HTTP forward proxy + CONNECT tunnel | ★★★★★ | Forward proxy GET + CONNECT tunnel + Basic auth (Proxy-Authorization header). Detects 407 auth challenges. /api/httpproxy/*. |
| Icecast | 8000 | Icecast streaming media server | ★★★★★ | /status-json.xsl gives mount points, listeners, stream metadata; admin stats with auth; source mount via ICY SOURCE protocol (authenticate + stream audio data). /api/icecast/status + /admin + /source. |
| Ident | 113 | Ident Protocol (RFC 1413) | ★★★★★ | Full RFC 1413: sends server-port/client-port pair, reads USERID response (OS type, encoding, username) or ERROR (no-user, hidden-user, etc.). Complete protocol. /api/ident/query. |
| IEC 60870-5-104 | 2404 | IEC 104 — SCADA/ICS telecontrol protocol | ★★★★★ | STARTDT/TESTFR + C_IC_NA_1 general interrogation + ASDU type/cause parsing for real telemetry reads + C_SC_NA_1/C_DC_NA_1 WRITE commands (single/double point command, setpoint by IOA). Full SCADA read+write. /api/iec104/probe + /read + /write. |
| Apache Ignite | 10800 | Apache Ignite — in-memory computing platform | ★★★★★ | Full thin client CRUD: list caches + GET/PUT/REMOVE with string keys/values. /api/ignite/list-caches + /cache-get + /cache-put + /cache-remove. |
| IKE | 500 | Internet Key Exchange — IPsec VPN negotiation | ★★★★★ | IKEv1 SA proposal + vendor ID extraction + IKEv2 SA_INIT with KE/Nonce payloads and cipher suite negotiation. /api/ike/probe + /version + /v2-sa. |
| IMAP | 143 | Internet Message Access Protocol (IMAP4) | ★★★★★ | LOGIN, LIST, SELECT with message count, plus interactive WebSocket session with full IMAP command set. |
| IMAPS | 993 | IMAP over TLS (implicit SSL) | ★★★★★ | TLS + LOGIN + LIST + SELECT + interactive WebSocket session with full IMAP command set over TLS. Feature parity with IMAP. |
| InfluxDB | 8086 | InfluxDB v2 — time-series database | ★★★★★ | /health + /ready, line protocol write, Flux query, bucket create/list/delete, org listing. /api/influxdb/buckets. |
| Informix | 9088 | IBM Informix database | ★★★★★ | SQLI connect packet (sqlexec/probe) + response detection + authenticated query via SQLI protocol with result row parsing. /api/informix/probe + /version + /query. No full TLI/NET transport negotiation. |
| IPFS | 4001 | IPFS — distributed filesystem (libp2p) | ★★★★★ | HTTP API: add file, cat file, node info/ID/peers + pin management (add/rm/ls) + pubsub (pub/ls topics). Full content-addressed storage + pub/sub workflow. /api/ipfs/add + /cat + /node-info + /pin-add + /pin-rm + /pin-ls + /pubsub-pub + /pubsub-ls. |
| IPMI | 623 | Intelligent Platform Management Interface | ★★★★★ | RMCP ASF Presence Ping (IPMI detection + entity type) + GetChannelAuthenticationCapabilities (auth methods: none/MD2/MD5/password/OEM, IPMI v2.0, KG required, anonymous login, per-message auth) + GetDeviceID (RMCP netFn=0x06, cmd=0x01: deviceId, firmwareVersion, IPMI version, manufacturerId/name HP/IBM/Dell/Supermicro, productId, SDR present). /api/ipmi/connect + /auth-caps + /device-id. IPMI uses UDP 623; TCP works on some BMCs. |
| IPP | 631 | Internet Printing Protocol | ★★★★★ | Get-Printer-Attributes (printer state, formats, capabilities) + Print-Job (0x0002) submission with document data, MIME type, job name, and job-id parsing. /api/ipp/probe + /api/ipp/print. |
| IRC | 6667 | Internet Relay Chat | ★★★★★ | Full registration, PING/PONG, MOTD, WebSocket session with JOIN/PART/PRIVMSG/NICK/QUIT/TOPIC/NAMES/LIST/WHOIS/MODE. |
| IRCS | 6697 | IRC over TLS | ★★★★★ | TLS + full IRC implementation with complete WebSocket command set. No functionality lost from plain IRC. |
| iSCSI | 3260 | iSCSI — SCSI over TCP (target discovery) | ★★★★★ | SendTargets discovery + CHAP login (MD5 challenge-response) to authenticate to a specific target. /api/iscsi/discover + /login. |
| Jabber Component | 5275 | XMPP component protocol (XEP-0114) | ★★★★★ | XML stream + SHA-1 handshake + IQ/message stanza send through the component connection + IQ roster get (jabber:iq:roster — enumerates contacts with JID/name/subscription/groups). /api/jabber-component/handshake + /send + /roster. |
| JDWP | 5005 | Java Debug Wire Protocol | ★★★★★ | Handshake + VirtualMachine.Version + IDSizes + VirtualMachine.AllThreads with thread names via ThreadReference.Name. Real JVM introspection. /api/jdwp/threads. |
| JetDirect | 9100 | HP JetDirect / AppSocket — raw printing | ★★★★★ | PJL INFO ID + STATUS queries + raw print job submission: plain text (PCL-wrapped), PCL, PostScript, and raw formats with PJL job boundaries. /api/jetdirect/connect + /api/jetdirect/print. |
| JSON-RPC | varies | JSON-RPC 2.0 over HTTP/WebSocket | ★★★★★ | Single and batch calls (HTTP POST) + WebSocket transport (blockchain nodes: Ethereum port 8546, Bitcoin port 8332) with correct frame encoding and Basic Auth. /api/jsonrpc/call + /batch + /ws. |
| Jupyter | 8888 | Jupyter Notebook/Lab REST API | ★★★★★ | /api/status, /api/kernelspecs + kernel lifecycle (create/list/delete) + notebook listing and content retrieval. Token auth. Full JupyterServer management. /api/jupyter/*. |
| Apache Kafka | 9092 | Apache Kafka — distributed event streaming | ★★★★★ | ApiVersions + Metadata (brokers/topics/partitions) + ProduceRequest v3 (RecordBatch) + FetchRequest v4 (consume messages: RecordBatch parser returns offset/timestamp/key/value + high-watermark) + ListGroups v0 (enumerate all consumer groups by ID + protocol type) + DescribeGroups v0 (group state/Stable/Empty/PreparingRebalance + member list with clientId/host). Full produce/consume/group-monitoring loop. /api/kafka/versions + /metadata + /produce + /fetch + /groups + /group-describe. |
| Kerberos | 88 | Kerberos authentication protocol (KDC) | ★★★★★ | Full ASN.1 DER AS-REQ + user enumeration (KRB-ERROR/PREAUTH_REQUIRED detection) + SPN enumeration (Kerberoasting target discovery). Core recon operations for AD environments. /api/kerberos/connect + /user-enum + /spn-check. |
| Kibana | 5601 | Kibana — Elasticsearch visualization platform | ★★★★★ | Status + saved objects + index patterns (v7/v8 API) + alerting rules + ES query proxy via /api/console/proxy. Basic/ApiKey auth. Full Kibana API coverage. /api/kibana/status + /saved-objects + /index-patterns + /alerts + /query. |
| Kubernetes | 6443 | Kubernetes API server | ★★★★★ | Health + /version + arbitrary API queries + pod logs + pod list + apply (create/update resources). Bearer token auth. Full ops coverage. /api/kubernetes/*. |
| L2TP | 1701 | Layer 2 Tunneling Protocol (RFC 2661) | ★★★★★ | Full SCCRQ/SCCRP/SCCCN handshake + session initiation (ICRQ→ICRP→ICCN) with AVP parsing (pure Uint8Array, no Node.js). Tunnel ID, peer hostname, protocol version, session IDs established. /api/l2tp/connect + /hello + /start-control + /session. |
| LDAP | 389 | Lightweight Directory Access Protocol | ★★★★★ | Full CRUD: bind + search + add entry + modify attributes (replace/add/delete) + delete entry. BER encoding for all operations. |
| LDAPS | 636 | LDAP over TLS | ★★★★★ | TLS + full CRUD identical to plaintext LDAP: bind/search/add/modify/delete. The standard for production directory work. |
| LDP | 646 | Label Distribution Protocol — MPLS signaling | ★★★★★ | Session init/keepalive + label mapping PDU (FEC → label binding distribution). /api/ldp/connect + /probe + /label-map. |
| Livestatus | 6557 | MK Livestatus — Nagios/Checkmk query interface | ★★★★★ | fixed16 response header, engine status, hosts query, services query (with state/output/ack columns), custom LQL, and external COMMAND writes (acknowledge, schedule downtime, reschedule checks). Full monitoring read+write access. /api/livestatus/status + /hosts + /services + /query + /command. |
| LLMNR | 5355 | Link-Local Multicast Name Resolution | ★★★★★ | RFC-correct TCP framing (2-byte length prefix) for A/AAAA forward + PTR reverse lookup + parallel hostname scanner (probe custom/default Windows hostname patterns concurrently). /api/llmnr/query + /reverse + /scan. LLMNR is primarily UDP; TCP fallback for large responses. |
| LMTP | 24 | Local Mail Transfer Protocol | ★★★★★ | LHLO, capability parsing, MAIL FROM + RCPT TO + DATA with per-recipient status codes — LMTP's key differentiator. /api/lmtp/connect + /send. |
| Loki | 3100 | Grafana Loki — log aggregation | ★★★★★ | Ready, build info, labels, LogQL instant + range queries, log push (streams), Prometheus scrape. /api/loki/push + /range. |
| LPD | 515 | Line Printer Daemon (RFC 1179) | ★★★★★ | Short/long queue state query + full RFC 1179 job submission (data file + control file with proper ack sequence) + job removal (0x05). /api/lpd/probe + /queue + /print + /remove. |
| LSP | varies | Language Server Protocol (JSON over stdio/TCP) | ★★★★★ | Full session: initialize→initialized→textDocument/didOpen→hover→completion→shutdown. Complete LSP lifecycle. /api/lsp/connect + /session. |
| ManageSieve | 4190 | ManageSieve — Sieve script management | ★★★★★ | Capabilities, SASL auth, LISTSCRIPTS, PUTSCRIPT (upload), GETSCRIPT (download), DELETESCRIPT, SETACTIVE. Full script lifecycle covered. |
| Matrix | 8448 | Matrix federated messaging protocol | ★★★★★ | Discovery + health check + login (m.login.password) + joined rooms list + arbitrary server query + message send + room create + room join. Full client-server API flow. /api/matrix/health + /login + /rooms + /query + /send + /room-create + /room-join. |
| MaxDB | 7210 | SAP MaxDB database | ★★★★★ | Full SAP NI protocol handshake: NI_CONNECT to global listener (D={database}\n\n\r) → X Server port discovery + NI_INFO (0xFF) service enumeration → lists all registered databases with X Server ports. Two-phase session via /api/maxdb/session: phase 1 NI_CONNECT to get X Server port, phase 2 connect to X Server for database-level session. /api/maxdb/connect + /api/maxdb/info + /api/maxdb/session. |
| mDNS | 5353 | Multicast DNS — local service discovery | ★★★★★ | Full DNS packet building (A/AAAA/PTR/SRV/TXT) with compression pointer parsing + service announcement (PTR+SRV+TXT response packet). /api/mdns/query + /discover + /announce. mDNS is UDP multicast; TCP is a fallback. |
| Meilisearch | 7700 | Meilisearch — fast search engine (HTTP API) | ★★★★★ | Health, version, stats, indexes, search, document add/update (batch), document delete (by IDs or all). /api/meilisearch/documents + /delete. |
| Memcached | 11211 | Memcached — distributed memory cache | ★★★★★ | VERSION, arbitrary commands (including storage ops with two-line format), WebSocket session, parsed STATS. Complete. |
| MGCP | 2427 | Media Gateway Control Protocol | ★★★★★ | AUEP (endpoint query) + arbitrary transaction commands + CRCX (create RTP connection with SDP offer) + DLCX (delete connection) — full call setup/teardown lifecycle. /api/mgcp/audit + /command + /call-setup. |
| Minecraft | 25565 | Minecraft Java Edition — server status ping | ★★★★★ | Full SLP: VarInt/VarLong codec, Handshake, Status Request, JSON response parsing, Ping/Pong latency. Complete. |
| MMS | 1755 | Microsoft Media Streaming protocol | ★★★★★ | Real MMST protocol: 8-byte preheader + 12-byte header with timestamp/command/direction fields. Sends CONNECT with player GUID + UTF-16LE transport string + DESCRIBE (stream metadata discovery: commandName, dataLength). Full MMS stream session. /api/mms/probe + /connect + /describe. |
| Modbus | 502 | Modbus TCP — industrial device protocol | ★★★★★ | Read coils/discrete inputs/holding registers/input registers + write single coil (FC0x05) + write multiple registers (FC0x10). Full read/write coverage for ICS work. |
| MongoDB | 27017 | MongoDB — document database (OP_MSG/BSON) | ★★★★★ | hello + buildInfo + ping + find (filter/limit/skip) + insertMany + updateOne/updateMany ($set/$unset/any operator) + deleteOne/deleteMany. Full CRUD with recursive BSON encoder. /api/mongodb/find + /insert + /update + /delete. |
| MPD | 6600 | Music Player Daemon | ★★★★★ | status, stats, currentsong, arbitrary commands + playback control: play/pause/stop/next/prev/seek, volume, queue add. Complete MPD client. /api/mpd/*. |
| MQTT | 1883 | MQTT 3.1.1 — IoT messaging protocol | ★★★★★ | CONNECT (with will, cleanSession, keepAlive, auth) + PUBLISH (QoS 0/1, retain flag, dup) + SUBSCRIBE (multi-topic, per-topic QoS) + UNSUBSCRIBE + PUBACK (QoS 1 ACK) + PINGREQ + DISCONNECT. Proper variable-length remaining-length encoding. WebSocket session: subscribe to topics, receive PUBLISH callbacks, publish ad-hoc. /api/mqtt/connect + /publish + /session. |
| MSN | 1863 | MSN Messenger Protocol (MSNP) | ★★★★★ | VER + CVR + USR TWN I (Tweener/Passport challenge for MSNP8-18) + MD5 auth (MSNP2-7: VER → INF → USR MD5 I → challenge → MD5(challenge+MD5(password)) → USR MD5 S → OK). Both auth flows for revival servers. /api/msn/probe + /version + /login + /md5-login. Service shut down in 2013. |
| MSRP | varies | Message Session Relay Protocol (SIP messaging) | ★★★★★ | SEND encoding + full session: SEND, REPORT, chunked transfer, session teardown. Complete MSRP messaging workflow. /api/msrp/send + /connect + /session. |
| Mumble | 64738 | Mumble VoIP server | ★★★★★ | Version exchange + Protobuf Authenticate + channel/user list (collects ChannelState/UserState until ServerSync) + TextMessage to channel. /api/mumble/probe + /version + /ping + /auth + /text-message. Voice requires UDP (unavailable). |
| Munin | 4949 | Munin node — performance monitoring | ★★★★★ | Banner, version, cap, list, and per-plugin value fetch with dot-terminator handling. Full munin-node protocol. |
| MySQL | 3306 | MySQL/MariaDB database | ★★★★★ | mysql_native_password + caching_sha2_password auth (MySQL 8+), COM_QUERY with result set parsing, SHOW DATABASES/TABLES. /api/mysql/*. |
| Napster | 8875 | OpenNap — Napster-compatible P2P network | ★★★★★ | Login + server stats + file search (OpenNap type 200/201/202) + browse user files (type 211/212/213). /api/napster/login + /stats + /search + /browse. |
| NATS | 4222 | NATS — cloud-native messaging | ★★★★★ | INFO/CONNECT + publish + subscribe (SUB/MSG collection) + request-reply + JetStream (stream create, publish, pull consumer). Full producer/consumer/persistence workflow. /api/nats/*. |
| NBD | 10809 | Network Block Device protocol | ★★★★★ | Full newstyle negotiation + export name + READ at configurable offset + WRITE block data. Full block device operations. /api/nbd/read + /write. |
| Neo4j | 7687 | Neo4j graph database (Bolt protocol) | ★★★★★ | Full Bolt RUN + PULL Cypher execution + parameterized queries + schema inspection + node/relationship create. /api/neo4j/query + /create. |
| NetBIOS | 139 | NetBIOS Session Service | ★★★★★ | Session layer connection to named resources + NBSTAT name query (node status, registered names, MAC address) + connectivity probe. /api/netbios/connect + /probe + /name-query. |
| NFS | 2049 | Network File System | ★★★★★ | Export listing + LOOKUP (resolve path to filehandle) + GETATTR (size, mtime, permissions) + READ (file content by filehandle + offset + count) + READDIR (list directory entries by filehandle) + WRITE (write data to file at offset). /api/nfs/exports + /lookup + /getattr + /read + /readdir + /write. |
| 9P | 564 | Plan 9 Filesystem Protocol (9P/Styx) | ★★★★★ | Full read-only filesystem: Tversion + Tattach + Twalk + Topen + Tread + Tstat + directory ls (stat record parsing). /api/9p/connect + /stat + /read + /ls. |
| NNTP | 119 | Network News Transfer Protocol | ★★★★★ | CAPABILITIES, MODE READER, GROUP, OVER, ARTICLE + LIST ACTIVE/NEWSGROUPS/OVERVIEW.FMT + POST (new article) + AUTHINFO USER/PASS. Full read/write newsgroup workflow. |
| NNTPS | 563 | NNTP over TLS | ★★★★★ | TLS + full NNTP coverage: list/post/auth. Same feature parity as plaintext NNTP. |
| Node Inspector | 9229 | Node.js V8 Inspector debugging protocol | ★★★★★ | HTTP session list + health check + WebSocket CDP tunnel for arbitrary debug commands (Runtime.evaluate, Debugger.*, etc.). Complete debug workflow. /api/node-inspector/health + /query + /tunnel. |
| Nomad | 4646 | HashiCorp Nomad — workload orchestration (HTTP API) | ★★★★★ | Health, leader, jobs/nodes list + allocations (filter by job/node/status) + deployments list + job dispatch (parameterized). Covers day-to-day ops work. |
| NRPE | 5666 | Nagios Remote Plugin Executor | ★★★★★ | Arbitrary check_commands with v2/v3 + TLS-encrypted queries (common production config). /api/nrpe/query + /tls. |
| NSCA | 5667 | Nagios Service Check Acceptor | ★★★★★ | XOR-encrypted passive check submission + AES-CBC-128 encrypted check submission (cipher 8). /api/nsca/send + /encrypted. |
| NSQ | 4150 | NSQ — real-time distributed messaging | ★★★★★ | IDENTIFY + PUB (single) + MPUB (atomic multi-publish) + DPUB (deferred publish with delay) + SUB with RDY/FIN message collection. Full producer/consumer workflow. /api/nsq/*. |
| NTP | 123 | Network Time Protocol v4 | ★★★★★ | Full NTPv4 response: stratum, precision, root delay/dispersion, reference ID, all timestamps. Multi-sample poll with min/max/avg offset + jitter (std dev) + RTT stats. /api/ntp/poll. |
| OPC UA | 4840 | OPC Unified Architecture — industrial automation | ★★★★★ | Hello→ACK→OPN→GetEndpoints + Read service (ReadRequest with NodeId, attribute, timestamps). Real data reads from OPC UA servers. /api/opcua/read + /endpoints. |
| OpenFlow | 6633 | OpenFlow — SDN controller protocol | ★★★★★ | FEATURES_REQUEST + GET_CONFIG + STATS_REQUEST (flow table, port, and aggregate statistics). /api/openflow/probe + /echo + /stats. |
| OpenTSDB | 4242 | OpenTSDB — time-series database (telnet API) | ★★★★★ | version + stats + suggest + telnet PUT command + HTTP /api/query with aggregator and tag filtering. Full read/write time-series coverage. /api/opentsdb/*. |
| OpenVPN | 1194 | OpenVPN VPN protocol (TCP mode) | ★★★★★ | HARD_RESET_CLIENT_V2 exchange + P_CONTROL_V1 wrapping a real TLS 1.2 ClientHello (6 cipher suites) → parses ServerHello cipher suite + Certificate presence. Full OpenVPN control channel + embedded TLS handshake. /api/openvpn/handshake + /tls. |
| Oracle TNS | 1521 | Oracle TNS Listener — full version detection | ★★★★★ | TNS Connect/Accept/Refuse + ANO negotiation + TTI login (username/password auth) + arbitrary SQL query execution (SELECT, DML, DDL) with result set parsing. /api/oracle-tns/connect + /probe + /query + /sql. |
| Oracle | 1521 | Oracle database (TNS detection wrapper) | ★★★★★ | TNS Connect/Accept/Refuse/Redirect with protocol version + service listing (COMMAND=STATUS, enumerates all registered services, instances, endpoints). /api/oracle/connect + /services. SQL execution via /api/oracle-tns/* endpoints. |
| OSCAR | 5190 | OSCAR — AIM/ICQ messaging protocol | ★★★★★ | Full FLAP+SNAC auth: signon → AuthKeyRequest → AuthKeyResponse (MD5 challenge) → MD5 password hash → AuthRequest → BOS server + auth cookie → BOS signon with cookie → ServerReady → ClientReady → ICBM SendIM (plaintext IM message via channel 1). /api/oscar/probe + /ping + /auth + /login + /send-im. Service defunct since 2017/2024; works with revival servers. |
| PCEP | 4189 | Path Computation Element Communication Protocol | ★★★★★ | OPEN session + connectivity probe + PCReq path computation request with ERO/RRO parsing (explicit/recorded route objects). /api/pcep/connect + /probe + /compute. |
| Perforce | 1666 | Perforce Helix Core VCS protocol | ★★★★★ | Tagged protocol + info + login with password-based authentication + changelist listing (p4 changes) + changelist describe with file list (p4 describe). /api/perforce/probe + /info + /login + /changes + /describe. |
| PJLink | 4352 | PJLink — projector control protocol | ★★★★★ | All Class 1 commands (name, power, input, errors, lamp hours, AV mute) + MD5 auth + power on/off control. |
| POP3 | 110 | Post Office Protocol 3 | ★★★★★ | USER/PASS auth, STAT + LIST, RETR + DELE (delete), UIDL (unique IDs), TOP (partial fetch), CAPA (capabilities). Complete POP3 client. /api/pop3/*. |
| POP3S | 995 | POP3 over TLS | ★★★★★ | TLS + full POP3 command set: auth, list, retrieve, DELE, UIDL, TOP, CAPA. Feature parity with plaintext POP3. /api/pop3s/*. |
| Portmapper | 111 | ONC RPC Portmapper / rpcbind | ★★★★★ | NULL probe (liveness) + DUMP to enumerate all registered program-to-port mappings + GETPORT to look up the TCP/UDP port for any specific RPC program/version. /api/portmapper/probe + /dump + /getport. |
| PostgreSQL | 5432 | PostgreSQL database | ★★★★★ | MD5 + SCRAM-SHA-256 auth (PostgreSQL 14+ default), Simple Query execution + table describe + LISTEN (async notification subscription with configurable wait window, collects NotificationResponse messages) + NOTIFY (pg_notify() dispatch). /api/postgres/query + /describe + /listen + /notify. |
| PPTP | 1723 | Point-to-Point Tunneling Protocol | ★★★★★ | SCCRQ/SCCRP tunnel handshake + full Outgoing-Call-Request/Reply (OCRQ→OCRP) with call ID/window/delay params + connect speed + peer call ID. /api/pptp/connect + /start-control + /call-setup. No GRE/PPP data plane. |
| Prometheus | 9090 | Prometheus — metrics and monitoring (HTTP API) | ★★★★★ | Health/readiness, build info, instant + range PromQL queries (with step/start/end), /metrics scrape. /api/prometheus/range. |
| QOTD | 17 | Quote of the Day Protocol (RFC 865) | ★★★★★ | Connect and receive the quote. That is the complete protocol. |
| Quake 3 | 27960 | Quake 3 / Source engine server query | ★★★★★ | getstatus + getinfo with server cvars and player list. UDP primary transport — some strict servers may not respond via TCP. |
| RabbitMQ | 15672 | RabbitMQ management HTTP API | ★★★★★ | Overview, nodes, queues, exchanges, connections, channels with Basic Auth + message publish via management API (/api/exchanges/{vhost}/{exchange}/publish). Full monitoring and message injection. /api/rabbitmq/health + /query + /publish. |
| RADIUS | 1812 | Remote Authentication Dial-In User Service | ★★★★★ | Full RFC 2865 Access-Request with MD5 PAP encryption + RFC 2866 Accounting-Request (Start/Stop/Interim with session stats: octets, packets, duration) over TCP (RFC 6613). No EAP or MSCHAP. /api/radius/probe + /auth + /accounting. |
| RadSec | 2083 | RADIUS over TLS (RadSec, RFC 6614) | ★★★★★ | TLS + RADIUS Access-Request with PAP credentials + Accounting-Request (Start/Stop/Interim-Update with session octets and duration). Feature parity with plain RADIUS over encrypted transport. /api/radsec/connect + /auth + /accounting. |
| RCON | 27015 | Source Engine / Minecraft RCON — server console | ★★★★★ | Full authentication + arbitrary command execution + response parsing. Complete RCON workflow. |
| RDP | 3389 | Remote Desktop Protocol | ★★★★★ | X.224 CR/CC with security protocol detection (Standard/TLS/NLA/RDSTLS) + NLA probe: TLS upgrade → CredSSP TSRequest → NTLM NEGOTIATE → parses NTLM CHALLENGE to extract Windows server identity (NetBIOS computer name, domain, DNS hostname/domain, server challenge nonce). /api/rdp/connect + /negotiate + /nla-probe. |
| RealAudio | 554 | RealMedia streaming (RTSP-based) | ★★★★★ | Full RTSP session: OPTIONS → DESCRIBE → SETUP → PLAY (with RTP-Info) → interleaved RTP frame collection → TEARDOWN. SDP track parsing, session ID. /api/realaudio/probe + /describe + /setup + /session. |
| Redis | 6379 | Redis in-memory data store | ★★★★★ | AUTH + SELECT, INFO, WebSocket interactive session, arbitrary RESP command execution. Complete. |
| RELP | 20514 | Reliable Event Logging Protocol | ★★★★★ | Session open + syslog message send with ACK + batch submit (multiple events in one RELP PDU). /api/relp/send + /batch. |
| RethinkDB | 28015 | RethinkDB — realtime database (Bolt/SCRAM) | ★★★★★ | V1.0 SCRAM auth (PBKDF2) + arbitrary ReQL query execution + table list + table create + document insert + server info + connection probe. /api/rethinkdb/connect + /probe + /query + /tables + /table-create + /insert + /info. |
| rexec | 512 | Remote Execute (BSD rexec) | ★★★★★ | Full command execution: auth, stdout capture, stderr secondary channel, WebSocket streaming. |
| Riak | 8087 | Riak KV — distributed key-value store (PBC) | ★★★★★ | PBC ping + server info + KV get (bucket/key/type, content-type extraction) + KV put (upsert with content type). /api/riak/get + /put. |
| RIP | 520 | Routing Information Protocol | ★★★★★ | RIPv1/v2 route request + response parsing + route update sending + RIPv2 authenticated update (RFC 2082 simple password, AFI=0xFFFF entry). /api/rip/request + /probe + /update + /send + /auth-update. RIP is UDP — most routers won't respond via TCP. |
| rlogin | 513 | Remote Login (BSD rlogin) | ★★★★★ | Handshake + banner grab + WebSocket streaming terminal. Complete for this obsolete protocol. /api/rlogin/connect + /banner. |
| RMI | 1099 | Java Remote Method Invocation | ★★★★★ | JRMI handshake + registry list() + remote method invocation on bound objects with argument marshaling + connection probe. /api/rmi/probe + /list + /invoke. |
| Rserve | 6311 | Rserve — R statistical server | ★★★★★ | QAP1 ID header parsing + connection probe + arbitrary R expression evaluation with full SEXP type decoding: strings, integer arrays (XT_ARRAY_INT), double arrays (XT_ARRAY_DOUBLE), logical arrays, generic vectors/lists. /api/rserve/probe + /eval. |
| RSH | 514 | Remote Shell (BSD rsh) | ★★★★★ | Full command execution + WebSocket streaming + .rhosts trust-probe (concurrent multi-user/host pair scanning to enumerate trusted pairs). /api/rsh/execute + /probe + /trust-scan. Note: privileged source port requirement not met in Workers. |
| rsync | 873 | rsync daemon | ★★★★★ | Module listing + challenge-response auth for protected modules + module-level access verification. /api/rsync/connect + /module + /auth. |
| RTMP | 1935 | Real-Time Messaging Protocol — video streaming | ★★★★★ | Handshake + connect + publish (stream key, FCPublish, NetStream.Publish.Start) + play (stream playback, onStatus). Full RTMP streaming workflow. /api/rtmp/publish + /play. |
| RTSP | 554 | Real Time Streaming Protocol | ★★★★★ | Full session: OPTIONS→DESCRIBE→SETUP (TCP interleaved)→PLAY→RTP frame collection→TEARDOWN. SDP track parsing, session ID. /api/rtsp/session. |
| S7comm | 102 | Siemens S7 — industrial PLC protocol | ★★★★★ | COTP + S7 setup + SZL read for CPU identification (model, serial, plant ID, firmware) + DB read (ReadVar request for arbitrary data block, byte range, hex output) + DB write (WriteVar with base64 data payload). /api/s7comm/connect, /read, /write. |
| SANE | 6566 | SANE Network Scanning protocol | ★★★★★ | SANE_NET_INIT (version/status) + SANE_NET_GET_DEVICES (enumerate scanners with vendor/model/type) + SANE_NET_OPEN (obtain device handle, detect auth resource) + GET_OPTION_DESCRIPTORS (all scanner options: resolution, color mode, paper size, constraints) + full scan workflow (SET_OPTION + GET_PARAMETERS + START + image data read from separate data port, up to 4 MB as base64). /api/sane/probe + /devices + /open + /options + /scan. |
| SCCP | 2000 | Skinny Client Control Protocol — Cisco VoIP | ★★★★★ | KeepAlive + RegisterMessage + line state query (ButtonTemplateReq, StimulusMessage) + call setup (OffHook + CapabilitiesResponse + KeypadButton digits → CallState/StartTone). /api/sccp/probe + /register + /line-state + /call-setup. |
| SCP | 22 | Secure Copy Protocol (SSH subsystem) | ★★★★★ | SSH banner grab + authenticated SSH exec channel: ls -la directory listing (structured file metadata) + full SCP wire protocol download (scp -f, C-message parsing, ACK, base64 file data) + upload (scp -t, control message + ACK handshake, base64 data). /api/scp/connect + /list + /get + /put. |
| Redis Sentinel | 26379 | Redis Sentinel — high-availability monitoring | ★★★★★ | PING, INFO, SENTINEL masters + KV get/set, get-master-addr-by-name, failover, reset, set config. /api/sentinel/*. |
| SFTP | 22 | SSH File Transfer Protocol | ★★★★★ | SSH banner grab + SFTP subsystem operations: list directory, download file, upload file, delete, mkdir, rename, stat (file attributes) via SSH WebSocket tunnel. /api/sftp/connect + /list + /download + /upload + /delete + /mkdir + /rename + /stat. |
| Shadowsocks | 8388 | Shadowsocks — encrypted censorship-circumvention proxy | ★☆☆☆☆ | TCP probe + 500ms silence detection heuristic. Without the encryption key, no meaningful interaction is possible. |
| SHOUTcast | 8000 | SHOUTcast — internet radio streaming (ICY protocol) | ★★★★★ | ICY header parsing + admin interface (listener count, stream info, kick/ban operations) + source mount via ICY SOURCE protocol (stream key auth, mount point, content-type). /api/shoutcast/probe + /info + /admin + /source. |
| SIP | 5060 | Session Initiation Protocol — VoIP signaling | ★★★★★ | OPTIONS + REGISTER + INVITE (with SDP offer, CANCEL cleanup) + Digest Auth (MD5 challenge-response for authenticated REGISTER/INVITE). Full SIP client workflow. /api/sip/options + /register + /invite + /digest-auth. |
| SIPS | 5061 | SIP over TLS | ★★★★★ | TLS + OPTIONS + REGISTER + INVITE with SDP offer + Digest Auth (RFC 2617 MD5 challenge-response over TLS: REGISTER→401→authenticated REGISTER). Full parity with SIP over encrypted transport. /api/sips/options + /register + /invite + /digest-auth. |
| SLP | 427 | Service Location Protocol | ★★★★★ | ServiceTypeRequest, ServiceRequest (by type), AttributeRequest — covers the primary service discovery use cases. |
| SMB | 445 | Server Message Block v2/v3 | ★★★★★ | SMB2 NEGOTIATE with full capabilities decode (DFS, leasing, large MTU, multi-channel, persistent handles), server GUID, system time + anonymous NTLM null-session authentication (SPNEGO-wrapped NTLMSSP_NEGOTIATE → NTLMSSP_AUTHENTICATE, reports guest/encrypted flags) + TREE_CONNECT to named share (returns share type DISK/PIPE/PRINT, share flags, capabilities, maximal access mask) + file stat (CREATE+QUERY_INFO+CLOSE, returns timestamps + fileAttributes). /api/smb/connect + /negotiate + /session + /tree + /stat. |
| SMPP | 2775 | Short Message Peer-to-Peer — SMS gateway protocol | ★★★★★ | bind_transceiver + submit_sm PDU (source addr, dest addr, message, data coding, registered_delivery) + query_sm (check delivery status by message_id). Returns SMSC-assigned message_id. /api/smpp/submit + /query. |
| SMTP | 25 | Simple Mail Transfer Protocol | ★★★★★ | EHLO with extension parsing, AUTH LOGIN, MAIL FROM, RCPT TO, DATA with headers and body, QUIT. Complete. |
| SMTPS | 465 | SMTP over TLS (implicit SSL) | ★★★★★ | TLS + identical full send flow as SMTP. AUTH + full email delivery over encrypted channel. |
| SNMP | 161 | Simple Network Management Protocol v1/v2c | ★★★★★ | Full ASN.1/BER GET + WALK + SNMPv3 authenticated GET (MD5/SHA HMAC, AES/DES privacy). /api/snmp/get + /walk + /v3-get. |
| SNPP | 444 | Simple Network Paging Protocol | ★★★★★ | PAGER, MESSAGE, SEND + optional HOLD and ALERT — can actually deliver pages to a paging gateway. |
| SOAP | 80 | Simple Object Access Protocol (XML web services) | ★★★★★ | WSDL discovery (service/operations/endpoints) + full SOAP envelope invocation with typed parameters. |
| SOCKS4 | 1080 | SOCKS4/4a proxy protocol | ★★★★★ | CONNECT request + SOCKS4A domain support (hostname extension) + relay: proxy HTTP/HTTPS requests through the SOCKS4 tunnel. Complete SOCKS4/4a workflow. /api/socks4/connect + /relay. |
| SOCKS5 | 1080 | SOCKS5 proxy protocol | ★★★★★ | Method negotiation, username/password auth (RFC 1929), CONNECT with IPv4/IPv6/domain + reply parsing + HTTP relay (verify end-to-end via SOCKS5 tunnel). /api/socks5/connect + /relay. |
| Solr | 8983 | Apache Solr — enterprise search | ★★★★★ | Health, /admin/info, /admin/cores + full query + document index (add/update, commit control) + delete (by ID or query). /api/solr/index + /delete. |
| Sonic | 1491 | Sonic — lightweight search backend | ★★★★★ | START control/ingest/search modes + QUERY (full-text search) + PUSH (index text) + SUGGEST (autocomplete) + PING + INFO. Complete Sonic client. /api/sonic/query + /push + /suggest. |
| SpamAssassin | 783 | SpamAssassin spamd — spam filtering | ★★★★★ | PING, CHECK, SYMBOLS, REPORT + TELL (train spam/ham with Message-class + Set/Remove headers). Full classification and training workflow. |
| SPDY | 443 | SPDY — deprecated HTTP/2 precursor | ★★★★★ | SPDY/3 SETTINGS probe + protocol detection (SPDY/HTTP2/HTTP1) + full HTTP/2 handshake: client preface → server SETTINGS parsing (HEADER_TABLE_SIZE, MAX_CONCURRENT_STREAMS, INITIAL_WINDOW_SIZE, MAX_FRAME_SIZE, etc.) → SETTINGS ACK → HEADERS GET / → response HEADERS (status code, Server: banner, content-type) via HPACK decode. /api/spdy/connect + /h2-probe. |
| SPICE | 5900 | SPICE — VM remote display protocol | ★★★★★ | Full SpiceLinkMess/SpiceLinkReply handshake + auth mechanism negotiation (SPICE password, SASL, or no-auth) + auth result + SpiceMainInit/SpiceMsgMainChannelsList parsing (enumerates display/cursor/inputs/playback/record channels). /api/spice/connect + /channels. No actual display session. |
| SSDP | 1900 | Simple Service Discovery Protocol (UPnP) | ★★★★★ | UPnP device description (rootDesc.xml + 9 fallback paths), M-SEARCH unicast, device XML parsing (type/manufacturer/model/services) + GENA event subscription (SUBSCRIBE/SID) + SOAP action invocation (GetExternalIPAddress, SetVolume, etc.). /api/ssdp/discover + /fetch + /search + /subscribe + /action. |
| SSH | 22 | Secure Shell | ★★★★★ | Banner grab + full SSH_MSG_KEXINIT exchange (kex/cipher/MAC/compression algorithm negotiation) + password/keyboard-interactive auth attempt + WebSocket TCP tunnel for browser ssh2 client. /api/ssh/connect + /kex + /auth + /terminal (WS). |
| STOMP | 61613 | Simple Text Oriented Messaging Protocol | ★★★★★ | CONNECT + SEND + SUBSCRIBE (collect N messages with timeout) + UNSUBSCRIBE + DISCONNECT. Auth and vhost support. /api/stomp/subscribe. |
| STUN | 3478 | Session Traversal Utilities for NAT (RFC 5389) | ★★★★★ | Full Binding Request with FINGERPRINT CRC32 + all attribute parsing (XOR-MAPPED-ADDRESS, ERROR-CODE, etc.). |
| SMTP Submission | 587 | SMTP email client submission (port 587) | ★★★★★ | EHLO + STARTTLS upgrade (startTls() API) + AUTH LOGIN + full MAIL FROM/RCPT TO/DATA flow. Complete authenticated email submission over TLS. /api/submission/send. |
| SVN | 3690 | Subversion VCS protocol | ★★★★★ | Greeting + capabilities + anonymous auth + directory listing with entry types + repos-root query + repository info (UUID, root URL, revision, last commit metadata). /api/svn/connect + /list + /info. |
| Sybase ASE | 5000 | Sybase Adaptive Server Enterprise (TDS-based) | ★★★★★ | Full TDS 5.0 login (XOR-0xA5 password, fixed-field layout) + SQL batch query with column/row result parsing + stored procedure execution (EXECUTE procname with typed params: strings auto-quoted/escaped, numerics unquoted). /api/sybase/login + /query + /proc. |
| Syslog | 514 | Syslog protocol (RFC 5424 and RFC 3164) | ★★★★★ | Both RFC 5424 (structured-data) and RFC 3164 (BSD syslog) formats with configurable facility and severity. |
| TACACS+ | 49 | TACACS+ AAA protocol | ★★★★★ | Complete auth exchange: START→REPLY→CONTINUE→REPLY with MD5 XOR encryption. Real credential testing. |
| Tarantool | 3301 | Tarantool — in-memory database (IProto/MessagePack) | ★★★★★ | Greeting + auth + IPROTO_EVAL (Lua expression) + IPROTO_EXECUTE (SQL). Full MessagePack encode/decode. /api/tarantool/eval + /sql. |
| TCP | varies | Raw TCP connection probe | ★★★★★ | Connect + optional send (utf8 or hex) + receive up to 65536 bytes + RTT + hex/text output. Versatile banner-grabber. |
| TDS | 1433 | Tabular Data Stream — SQL Server (pre-login) | ★★★★★ | Pre-Login (version, encryption) + TDS login7 auth + SQL query execution with full result set parsing (25+ type tokens). /api/tds/login + /query. |
| TeamSpeak 3 | 10011 | TeamSpeak 3 ServerQuery interface | ★★★★★ | ServerQuery read-only commands + channel list/info + write commands (move, kick, message). /api/teamspeak/command + /channel. |
| Telnet | 23 | Telnet protocol | ★★★★★ | Banner grab + IAC option negotiation (WILL/WONT/DO/DONT) + TERMINAL-TYPE + NAWS + interactive TCP tunnel + credential login (username/password prompt detection, post-auth response capture). /api/telnet/connect + /negotiate + /login. |
| TFTP | 69 | Trivial File Transfer Protocol | ★★★★★ | RRQ/WRQ/DATA/ACK packet encoding, block transfer, error code parsing + RFC 2347 options negotiation (blksize/timeout/tsize with OACK parsing). /api/tftp/connect + /read + /write + /get + /options. TCP-only — standard TFTP runs over UDP. |
| Apache Thrift | 9090 | Apache Thrift — cross-language RPC framework | ★★★★★ | Full Thrift Binary Protocol: probe (any method, empty args) + custom calls with typed parameters. Framed and buffered. |
| Time | 37 | Time Protocol (RFC 868) | ★★★★★ | 4-byte response → Unix epoch → ISO 8601 + clock offset calculation. The complete protocol. |
| Tor Control | 9051 | Tor Control Protocol | ★★★★★ | PROTOCOLINFO probe (auth methods, Tor version) + authenticated GETINFO with configurable keys + SIGNAL (NEWNYM, RELOAD, CLEARDNSCACHE, HALT) with pre/post circuit-status. /api/torcontrol/probe + /getinfo + /signal. |
| TURN | 3478 | Traversal Using Relays around NAT (RFC 5766) | ★★★★★ | TURN ALLOCATE (relay endpoint with HMAC-SHA1 auth) + CreatePermission (whitelist peer IP) + connectivity probe. /api/turn/allocate + /probe + /permission. |
| UUCP | 540 | UNIX-to-UNIX Copy protocol | ★★★★★ | Wakeup + Shere greeting + system name exchange + handshake detection (DLE+S raw UUCP vs login-gated). Complete for this obsolete protocol. |
| uWSGI | 3031 | uWSGI application server protocol | ★★★★★ | Binary packet with WSGI environment vars + HTTP response parsing. Custom request endpoint for any path/method. |
| Varnish | 6082 | Varnish Cache management CLI (VCLI) | ★★★★★ | SHA-256 auth + read-only monitoring (ping, status, backend.list, vcl.list, param.show) + write ops: ban URLs, set params. Full cache management. /api/varnish/*. |
| HashiCorp Vault | 8200 | Vault — secrets management (HTTP API) | ★★★★★ | Health, seal-status, arbitrary sys queries + KV secret read (v1/v2 auto-detect, versioned) + secret write. /api/vault/secret/read + /write. |
| Ventrilo | 3784 | Ventrilo VoIP — proprietary protocol | ★☆☆☆☆ | Pure port probe + best-effort parse of undocumented binary. Superseded by Discord; no useful output. |
| VNC | 5900 | VNC — Remote Framebuffer Protocol | ★★★★★ | RFB version negotiation + security type enumeration + VNC password auth (DES challenge-response). /api/vnc/connect + /auth. |
| WebSocket | 80 | WebSocket protocol (RFC 6455) | ★★★★★ | Full handshake with Sec-WebSocket-Accept SHA-1 validation, subprotocol/extension negotiation, optional masked ping. |
| WHOIS | 43 | WHOIS protocol (RFC 3912) | ★★★★★ | Automatic TLD-to-server routing (40+ TLDs) + referral chasing (registry→registrar, one hop) + structured field parsing (registrar, dates, nameservers, status, contacts) + IP/ASN WHOIS with ARIN/RIPE/APNIC/LACNIC/AFRINIC routing and ReferralServer chasing. /api/whois/lookup + /api/whois/ip. |
| WinRM | 5985 | Windows Remote Management (WS-Management/SOAP) | ★★★★★ | Identify + auth probe + remote command execution (WSMAN Shell: cmd.exe, PowerShell). /api/winrm/identify + /auth + /exec. |
| X11 | 6000 | X Window System (X11) | ★★★★★ | Full Setup + Success response: vendor, release, resource IDs, screens with dimensions and pixel formats + QueryTree window enumeration (root window child windows with IDs and counts). /api/x11/connect + /query-tree. |
| XMPP | 5222 | XMPP client protocol (Jabber) | ★★★★★ | SASL PLAIN auth + stream restart + resource bind + roster fetch + message stanza send. Full client workflow. /api/xmpp/login, /roster, /message. |
| XMPP S2S | 5269 | XMPP server-to-server federation | ★★★★★ | S2S stream open + feature detection + XEP-0220 dialback key generation/verification + ping (IQ ping over S2S) + full federation connectivity test. /api/xmpp-s2s/ping + /connect + /s2s-connect + /dialback + /api/xmpps2s/probe + /federation. |
| XMPP S2S (TLS) | 5269 | XMPP S2S with STARTTLS upgrade + dialback | ★★★★★ | Plain stream open → STARTTLS negotiation → socket.startTls() upgrade → new stream on TLS → XEP-0220 dialback key exchange (valid/invalid/pending/error result). Falls back to plain dialback if STARTTLS not offered. /api/xmpps2s/probe + /federation + /dialback. |
| YMSG | 5050 | Yahoo Messenger Protocol | ★★★★★ | PING + AuthReq (service 0x4B) → challenge seed (key 94) → MD5 auth response: p=MD5(password), y_hash=MD5(seed+p), c_hash=MD5(y_hash+seed) → AuthResp (service 0x54) with Y+C hashes → login result. Full YMSG16 auth flow for revival servers. /api/ymsg/probe + /version + /auth + /login. Service shut down July 2018. |
| Zabbix | 10050 | Zabbix monitoring — agent and server protocol | ★★★★★ | Agent check requests + passive item queries + LLD discovery rules (system.run, discovery data parsing). /api/zabbix/agent + /discovery. |
| ZMTP | 5555 | ZeroMQ Message Transport Protocol 3.1 | ★★★★★ | Greeting + READY exchange + message send (REQ/PUSH/DEALER) with reply collection + subscribe (SUB) with topic filter and frame collection. Full ZMTP messaging. /api/zmtp/send + /recv. |
| ZooKeeper | 2181 | Apache ZooKeeper — distributed coordination | ★★★★★ | ruok/srvr/4LW commands + full Jute binary protocol: ConnectRequest + getData + setData + create znode. Real znode operations, not just admin commands. |
